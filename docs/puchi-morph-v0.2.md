# Picture Score — 絵→音符モーフ v0.2

`絵 ↔ 音楽` スライダーを、音の解釈だけでなく表示の解釈にも接続する。

- 左端: 元の線を主役にし、ぷち音符は各ストロークにごく少数だけ残す。
- 中央: 線とぷち音符が共存する。
- 右端: 元の線をフェードアウトし、間引いたぷち音符だけで元絵の形を残す。
- 表示用の音符選択は決定論的・均等配置で、解析点を全表示しない。
- 音楽生成、演奏アンカー、タイミング、保存Projectは変更しない。

実機QAでは、左→中央→右へ動かしたときに「絵 → 絵＋音符 → 猫の形をした楽譜」と連続的に読めることを確認する。
